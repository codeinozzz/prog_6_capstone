import { Routes } from '@angular/router';
import { TankContainerComponent } from './features/tank/tank-container.component';

export const routes: Routes = [
  { path: '', component: TankContainerComponent }
];
